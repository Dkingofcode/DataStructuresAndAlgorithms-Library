#ifndef SEARCHING_HPP
#define SEARCHING_HPP

#include <vector>

class Sorting {
    public: 
      static int linearSearch(std::vector<int>& array, int key);

      static int binarySearch(std::vector<int>& arr, int key);

      
      
};

#include "searching.cpp"

#endif

























// #ifndef SORTING_HPP
// #define SORTING_HPP

// #include <vector>

// class Sorting {
// public:
//     static void bubbleSort(std::vector<int>& arr);
// };

// #include "sorting.cpp"

// #endif
